plugins {
    id 'org.asciidoctor.jvm.convert' version '3.3.2'
    id 'org.asciidoctor.jvm.gems' version '3.3.2'

    id 'java'
}

group 'de.renatius.tdd-playground.tipps'
version '0.1'

repositories {
    ruby.gems()
}

dependencies {
    testImplementation(
            'org.junit.jupiter:junit-jupiter-api',
            'org.mockito:mockito-core',
            libs.assertJ.core.get(),
    )

    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine'
}

asciidoctor {
    dependsOn asciidoctorGemsPrepare

    baseDirIsProjectDir()
    baseDirFollowsSourceFile()

    attributes 'build-gradle': file('build.gradle'),
            'sourceDir': project.sourceSets.main.java.srcDirs[0],
            'javaSourceDir': project.sourceSets.main.java.srcDirs[0],
            'javaTestDir': project.sourceSets.test.java.srcDirs[0],

            'author': 'René Six <dev@renatius.de>',
            'icons': 'font',
            'imagesdir': '{docdir}/images',
            'languages': ['de', 'en'],
            'revnumber' : { project.version.toString() },
            'sectanchors': true,
            'sectlinks': true,
            'sectnums': true,
            'source-highlighter': 'rouge',
            'rouge-style': 'monokai',
            //'source-highlighter': 'pygments',
            //'pygments-style': 'emacs',
            'toc': 'right'
}

build.dependsOn asciidoctor
